box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: herring-cove
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: squallcx.github.io
        basedir: public



